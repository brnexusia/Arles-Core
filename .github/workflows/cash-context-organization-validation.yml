name: Cash context organization validation

on:
  push:
    branches:
      - fix/cash-context-organization

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          DATABASE_URL: postgresql://test:test@127.0.0.1:5432/test
          REDIS_URL: redis://127.0.0.1:6379
          EVOLUTION_BASE_URL: http://127.0.0.1:8080
          EVOLUTION_API_KEY: test
      - name: Run targeted cash context tests
        id: targeted
        continue-on-error: true
        run: npx vitest run tests/cash-context-organization.test.ts --reporter=json --outputFile=vitest-results.json
        env:
          DATABASE_URL: postgresql://test:test@127.0.0.1:5432/test
          REDIS_URL: redis://127.0.0.1:6379
          EVOLUTION_BASE_URL: http://127.0.0.1:8080
          EVOLUTION_API_KEY: test
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cash-context-vitest-results
          path: vitest-results.json
      - name: Fail when targeted tests failed
        if: steps.targeted.outcome == 'failure'
        run: exit 1
